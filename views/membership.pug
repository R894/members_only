extends layout

block content 
    form(action="", method="post")
        label(for="code") Enter the secret membership code:
        input(id="code", name="code")
        br
        button(type="submit") Submit

        if errors
            ul
                for err in errors
                    li!= err.msg